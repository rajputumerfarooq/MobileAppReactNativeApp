//import liraries
import React, { Component } from 'react';
import { View, Text, StyleSheet } from 'react-native';

//Screens
import {default as MobileSubCategories} from './Mobile/SubCategories'

// create a component
const index = () => {
    return (

        <MobileSubCategories/>

    );
};


//make this component available to the app
export default index;
