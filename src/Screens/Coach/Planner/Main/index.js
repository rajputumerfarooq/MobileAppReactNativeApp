//import liraries
import React, { Component } from 'react';
import { View, Text, StyleSheet } from 'react-native';
import Planner from './Mobile/Planner';


// create a component
const index = () => {
    return (
        <Planner/>
    );
};


//make this component available to the app
export default index;
